spring:
  datasource:
    url: jdbc:postgresql://ep-wispy-truth-a8xs50j7-pooler.eastus2.azure.neon.tech/neondb?sslmode=require
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop # Garante um schema limpo para cada execução
    show-sql: true
  test:
    database:
      replace: none
